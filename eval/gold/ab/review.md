# Inner Chapter A/B — six dimensions

A: `eval/gold/original.pptx`  
B: `eval/gold/ab/optimized.pptx`  
Rule: equal = lose. Facts in B must stay inside A's extract. Human review; not CI.

| Dimension | Verdict | Evidence |
|---|---|---|
| format | **win** | A uses topic titles (`Agenda`, `Background`, `Gemma 4 Overview`, `Key Numbers`) and a `Thank You` closer. B uses takeaway sentences in zh-Hant with English terms; no thank-you closer. |
| layout | **win** | A parks most slides on `content-centered-a`. B uses cover, title-body, `title-centered` + three native rounded rects, then three `column-2-centered` model slides. Same Inner Chapter master. |
| narrative | **win** | A is a catalog (Introduction → four models → Conclusion). B titles in order: catalog without a decision → 2026 list is not a long-context claim → three parallel knobs already in the draft → Gemma / Laguna / ZAYA1 / V4 salvaged → Key Numbers has no numbers → start from KV sharing before CSA/HCA. |
| depth | **win** | No new numbers (lock holds: no 2.7 GB). Depth win is honesty: A’s “Key Numbers” has none; B names KV sharing, PLE, GQA, 26B/31B, 40 layers, CCA/MLA, mHC, CSA/HCA as the draft’s actual payload instead of “Saves memory”. |
| logic | **win** | A implies “better models in 2026”. B argues: the draft already named three knobs, buried them per model, then closed with empty metrics. Decision follows from what A already listed. |
| story | **win** | Titles alone tell the argument. No Thank You. Does not smuggle sourced depth into this pair. |

**pass:** yes (six wins). Same visual system.
